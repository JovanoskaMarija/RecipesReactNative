import { StyleSheet } from 'react-native'

const StyleFavourites = StyleSheet.create({
    gridFavourites: {
        display: 'flex',
        // alignItems: 'center',
        // justifyContent: 'center'
    }

})

export default StyleFavourites